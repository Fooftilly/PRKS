# Maintainer / browser-test dependencies. Not used by Docker or production runtime.
playwright==1.63.0
# Contract-test helpers for the typed API boundary (#180 / #45). Required by
# the Unit / API / contract Test Gate job (installed alongside runtime pins);
# not used by Docker or production runtime.
openapi-core==0.23.1
