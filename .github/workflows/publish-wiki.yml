name: Publish Wiki

on:
  push:
    branches: [master]
    paths:
      - "docs/wiki/**"
      - ".github/workflows/publish-wiki.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Publish docs/wiki to GitHub Wiki
        env:
          GH_TOKEN: ${{ github.token }}
          WIKI_BASE_URL: https://github.com/${{ github.repository }}/wiki
        shell: bash
        run: |
          set -euo pipefail

          wiki_url="https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.wiki.git"

          if ! git ls-remote "$wiki_url" HEAD >/dev/null 2>&1; then
            echo "::notice::GitHub Wiki is enabled but has not been initialized yet."
            echo "::notice::Create the first Wiki page once in GitHub, then rerun this workflow."
            exit 0
          fi

          git clone --depth 1 "$wiki_url" .wiki

          find .wiki -maxdepth 1 -type f -name '*.md' -delete
          for source in docs/wiki/*.md; do
            if [[ "$(basename "$source")" == "README.md" ]]; then
              continue
            fi
            cp "$source" ".wiki/$(basename "$source")"
          done

          # Repository source uses *.md links so docs/wiki is browsable in normal
          # GitHub code views. GitHub Wiki Markdown links should target rendered
          # wiki page URLs instead, so rewrite only local page links in the copy.
          python - <<'PY'
          import os
          import re
          from pathlib import Path

          wiki_dir = Path(".wiki")
          wiki_base = os.environ["WIKI_BASE_URL"].rstrip("/")
          link_re = re.compile(
              r"(?P<prefix>\]\()(?P<page>[A-Za-z0-9_-]+)\.md"
              r"(?P<anchor>#[^)]*)?(?P<suffix>\))"
          )

          for path in wiki_dir.glob("*.md"):
              content = path.read_text(encoding="utf-8")
              content = link_re.sub(
                  lambda match: (
                      f'{match.group("prefix")}{wiki_base}/{match.group("page")}'
                      f'{match.group("anchor") or ""}{match.group("suffix")}'
                  ),
                  content,
              )
              path.write_text(content, encoding="utf-8")

          relative_md_link = re.compile(r"\]\((?!https?://|mailto:|#)[^)]+\.md(?:#[^)]*)?\)")
          unresolved = []
          for path in wiki_dir.glob("*.md"):
              for match in relative_md_link.finditer(path.read_text(encoding="utf-8")):
                  unresolved.append(f"{path.name}: {match.group(0)}")

          if unresolved:
              raise SystemExit(
                  "Published wiki still contains repository-style Markdown links:\n"
                  + "\n".join(unresolved)
              )
          PY

          cd .wiki
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A

          if git diff --cached --quiet; then
            echo "Wiki already matches docs/wiki."
            exit 0
          fi

          git commit -m "docs: sync wiki from ${GITHUB_SHA}"
          git push origin HEAD:master
